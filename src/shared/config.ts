// Here you would provide configuration for the application.
// This could include things such as API keys, database connection strings
// token lifetime

export const config = { env: 'development' };
